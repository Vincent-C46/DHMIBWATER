<Window x:Class="DHBIMWATER.UI.Views.Modeling.Modeling1View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:controls="clr-namespace:DHBIMWATER.UI.Controls"
        Title="DHBIM WATER - UI Kit" 
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F4F7F9" 
        AllowsTransparency="False" 
        WindowStyle="None"
        FontFamily="Segoe UI, Malgun Gothic">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DHBIMWATER.UI;component/Styles/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" CornerRadius="15" GlassFrameThickness="1" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <controls:TitleBar Grid.Row="0"/>

        <Grid Grid.Row="1" Margin="30,10,30,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="15" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#DDE4EB" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel Margin="30">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                        <Border Background="#E3F2FD" CornerRadius="8" Padding="8">
                            <iconPacks:PackIconMaterial Kind="Water" Width="20" Height="20" Foreground="#2196F3"/>
                        </Border>
                        <TextBlock Text="DH BIM WATER" FontSize="18" FontWeight="Bold" Margin="12,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <iconPacks:PackIconMaterial Kind="RocketLaunch" Width="50" Height="50" Foreground="#34495E" HorizontalAlignment="Center" Margin="0,40,0,20"/>
                    <TextBlock Text="Welcome!" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="아래 컨트롤들을 활용하여&#x0a;스마트한 기능을 개발하세요." TextAlignment="Center" Foreground="#7F8C8D" Margin="0,10,0,30"/>

                    <Button Style="{StaticResource PrimaryButtonStyle}" Content="튜토리얼 시작하기" Height="40"/>

                    <GroupBox Header="시스템 정보" Style="{StaticResource PrimaryGroupBox}" Margin="0,30,0,0">
                        <StackPanel>
                            <CheckBox Content="자동 업데이트 활성화" Style="{StaticResource PrimaryCheckBoxStyle}" IsChecked="True" Margin="0,5"/>
                            <CheckBox Content="디버그 로그 남기기" Style="{StaticResource PrimaryCheckBoxStyle}" Margin="0,5"/>
                            <ProgressBar Style="{StaticResource PrimaryProgressBarStyle}" Value="75" Margin="0,15,0,5"/>
                            <TextBlock Text="리소스 사용량: 75%" FontSize="10" HorizontalAlignment="Right" Foreground="#95A5A6"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="15" Margin="10,0,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#DDE4EB" Opacity="0.3"/>
                </Border.Effect>

                <TabControl Style="{StaticResource PrimaryTabControlStyle}" Margin="20">
                    <TabItem Header="Inputs &amp; Lists" Style="{StaticResource PrimaryTabItemStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="텍스트 입력" FontWeight="Bold" Margin="0,0,0,8"/>
                                        <TextBox Style="{StaticResource PrimaryTextBoxStyle}" Tag="프로젝트 이름을 입력하세요..."/>

                                        <TextBlock Text="카테고리 선택" FontWeight="Bold" Margin="0,15,0,8"/>
                                        <ComboBox Style="{StaticResource PrimaryComboBoxStyle}">
                                            <ComboBoxItem Content="배관 모델링"/>
                                            <ComboBoxItem Content="덕트 모델링"/>
                                            <ComboBoxItem Content="전기 트레이"/>
                                        </ComboBox>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                        <TextBlock Text="항목 리스트" FontWeight="Bold" Margin="0,0,0,8"/>
                                        <ListBox Style="{StaticResource PrimaryListBoxStyle}" Height="120">
                                            <ListBoxItem Content="Family_Pipe_Standard_01"/>
                                            <ListBoxItem Content="Family_Pipe_Standard_02"/>
                                            <ListBoxItem Content="Family_Pipe_Special_Type"/>
                                        </ListBox>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Data Table" Style="{StaticResource PrimaryTabItemStyle}">
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="파라미터 일괄 편집" FontWeight="Bold" Margin="0,0,0,10"/>

                            <DataGrid Grid.Row="1" Style="{StaticResource PrimaryDataGridStyle}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="100"/>
                                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#E9ECEF" Margin="30,0,30,15" CornerRadius="0,0,10,10">
            <DockPanel Margin="15,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <iconPacks:PackIconMaterial Kind="InformationOutline" Width="14" Height="14" Foreground="#95A5A6"/>
                    <TextBlock Text="v1.0.0 | Developer: DH BIM Team" Margin="8,0,0,0" FontSize="11" Foreground="#7F8C8D"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Fill="#27AE60" Margin="0,0,8,0"/>
                    <TextBlock Text="All Systems Functional" FontSize="11" Foreground="#27AE60" FontWeight="Bold"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>