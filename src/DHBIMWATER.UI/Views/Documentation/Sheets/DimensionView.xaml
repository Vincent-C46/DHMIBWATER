<Window x:Class="DHBIMWATER.UI.Views.Documentation.Sheets.DimensionView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:DHBIMWATER.UI.Controls"
        xmlns:behaviors="clr-namespace:DHBIMWATER.UI.Behaviors"
        behaviors:DialogResultBehavior.DialogResult="{Binding DialogResult}"
        mc:Ignorable="d"
        Title="Dimension"
        Height="205" Width="200"
        WindowStartupLocation="CenterScreen"
        Background="#F4F7F9"
        AllowsTransparency="False"
        WindowStyle="None"
        FontFamily="Segoe UI, Malgun Gothic">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" CornerRadius="15" GlassFrameThickness="1" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DHBIMWATER.UI;component/Styles/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="80"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <controls:TitleBar Grid.Row="0" Margin="0,0,0,0"/>

        <Grid Grid.Row="1" Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" HorizontalAlignment="Left">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
            </Grid>
            <Border Grid.Row="0">
                <RadioButton
                    Content="현재 뷰 치수선 작성"
                    VerticalAlignment="Center"
                    IsChecked="{Binding IsAllObjectsMode, Mode=TwoWay}" 
                />

            </Border>
            <Border Grid.Row="1">
                <RadioButton
                    Content="선택 객체 치수선 작성"
                    VerticalAlignment="Center"
                    IsChecked="{Binding IsSelectedObjectsMode, Mode=TwoWay}"
                />
            </Border>
        </Grid>

        <!-- ComboBox -->

        <Grid Grid.Row="2" VerticalAlignment="Center">
            <StackPanel>
                <ComboBox Margin="5,0,5,5"
                  Width="180" Height="35"
                  Style="{StaticResource PrimaryComboBoxStyle}"
                  ItemsSource="{Binding DimensionTypes}"
                  SelectedItem="{Binding SelectedDimensionType}"
                  DisplayMemberPath="Name"/>
            </StackPanel>
        </Grid>

        <!-- Bottom: Buttons -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0,5,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="OK"
                        Width="80" FontSize="15"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding ConfirmCommand}"
                        IsDefault="True"/>
                
                <Button Grid.Column="1" Content="Cancel"
                        Width="80" FontSize="15"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding CancelCommand}"
                        Margin="5,0,0,0" IsCancel="True"/>
            </Grid>
        </Grid>
    </Grid>
</Window>