<Window x:Class="DHBIMWATER.UI.Views.Modeling.PipeLayoutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:DHBIMWATER.UI.Controls"
        xmlns:vm="clr-namespace:DHBIMWATER.UI.ViewModels.Modeling"
        mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:PipeLayoutViewModel}"
        Title="DHBIM WATER - Pipe Layout" Width="1100" Height="720" WindowStartupLocation="CenterScreen"
        Background="#F4F7F9" WindowStyle="None" FontFamily="Segoe UI, Malgun Gothic">
    <Window.Resources><ResourceDictionary><BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="/DHBIMWATER.UI;component/Styles/Generic.xaml"/></ResourceDictionary.MergedDictionaries></ResourceDictionary></Window.Resources>
    <WindowChrome.WindowChrome><WindowChrome CaptionHeight="0" CornerRadius="15" GlassFrameThickness="1" UseAeroCaptionButtons="False"/></WindowChrome.WindowChrome>
    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <controls:TitleBar Grid.Row="0"/>
        <Grid Grid.Row="1" Margin="20,12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="260"/></Grid.ColumnDefinitions>
            <Border Background="White" CornerRadius="12" Padding="8" Margin="0,0,12,0"><Grid>
                <Canvas x:Name="LayoutCanvas" Background="#FCFDFE" MouseLeftButtonDown="OnCanvasMouseLeftButtonDown" MouseMove="OnCanvasMouseMove" SizeChanged="OnLayoutCanvasSizeChanged">
                    <Line X1="{Binding ReferenceScreenX}" Y1="0" X2="{Binding ReferenceScreenX}" Y2="{Binding ActualHeight, ElementName=LayoutCanvas}" Stroke="#9ACD32" StrokeThickness="1.5" StrokeDashArray="4,3" IsHitTestVisible="False"/>
                    <Line X1="0" Y1="{Binding ReferenceScreenY}" X2="{Binding ActualWidth, ElementName=LayoutCanvas}" Y2="{Binding ReferenceScreenY}" Stroke="#9ACD32" StrokeThickness="1.5" StrokeDashArray="4,3" IsHitTestVisible="False"/>
                    <Line X1="{Binding PreviewX1}" Y1="{Binding PreviewY1}" X2="{Binding PreviewX2}" Y2="{Binding PreviewY2}" Stroke="#E67E22" StrokeThickness="3" StrokeDashArray="6,3" IsHitTestVisible="False" Visibility="{Binding IsPreviewVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <ItemsControl ItemsSource="{Binding Edges}" IsHitTestVisible="True"><ItemsControl.ItemsPanel><ItemsPanelTemplate><Canvas/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Line X1="{Binding X1}" Y1="{Binding Y1}" X2="{Binding X2}" Y2="{Binding Y2}" Stroke="#2878B8" StrokeThickness="4" MouseLeftButtonDown="OnEdgeMouseLeftButtonDown"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <ItemsControl ItemsSource="{Binding InlineFittings}" IsHitTestVisible="False"><ItemsControl.ItemsPanel><ItemsPanelTemplate><Canvas/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Border Canvas.Left="{Binding X}" Canvas.Top="{Binding Y}" Width="14" Height="14" Background="#2C3E50" CornerRadius="7" ToolTip="{Binding TypeKey}"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <ItemsControl ItemsSource="{Binding Nodes}" IsHitTestVisible="False"><ItemsControl.ItemsPanel><ItemsPanelTemplate><Canvas/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><StackPanel Canvas.Left="{Binding X}" Canvas.Top="{Binding Y}" Margin="-7,-7,0,0"><Ellipse Width="14" Height="14" Fill="{Binding Color}" Stroke="White" StrokeThickness="2"/><TextBlock Text="{Binding Label}" FontSize="10" Foreground="#34495E"/></StackPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                </Canvas>
                <Border VerticalAlignment="Top" HorizontalAlignment="Left" Background="#EAF2F8" CornerRadius="5" Padding="8"><TextBlock Text="연두색 십자점은 입력한 기준점입니다. 빈 화면에서는 기준점만 100mm 스냅됩니다. 노드: 회색 Cap · 파랑 Inline · 주황 Elbow · 보라 Tee · 빨강 Cross" FontSize="11"/></Border>
            </Grid></Border>
            <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="16"><StackPanel>
                <TextBlock Text="인라인 부속품" FontWeight="Bold" Foreground="#4A6FA5" FontSize="16"/>
                <TextBlock Text="선택 엣지에 순서대로 추가됩니다." Foreground="#7F8C8D" FontSize="11" Margin="0,4,0,12"/>
                <CheckBox Content="직교 + 45° 각도 스냅" IsChecked="{Binding UseAngleSnap}" Margin="0,0,0,8"/>
                <TextBlock Text="기준점 좌표" FontWeight="SemiBold"/>
                <Grid Margin="0,4,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="75"/><ColumnDefinition Width="24"/></Grid.ColumnDefinitions><TextBlock Text="X" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding ReferenceX, UpdateSourceTrigger=LostFocus}"/><TextBlock Grid.Column="2" Text="mm" Margin="4,0"/></Grid>
                <Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="75"/><ColumnDefinition Width="24"/></Grid.ColumnDefinitions><TextBlock Text="Y" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding ReferenceY, UpdateSourceTrigger=LostFocus}"/><TextBlock Grid.Column="2" Text="mm" Margin="4,0"/></Grid>
                <TextBlock Text="출력 방식" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding OutputModes}" SelectedItem="{Binding OutputMode}" Style="{StaticResource PrimaryComboBoxStyle}" Margin="0,4,0,8"/>
                <Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="75"/><ColumnDefinition Width="24"/></Grid.ColumnDefinitions><TextBlock Text="표고" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding Elevation, UpdateSourceTrigger=LostFocus}"/><TextBlock Grid.Column="2" Text="mm" Margin="4,0"/></Grid>
                <Grid Margin="0,0,0,10"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="75"/><ColumnDefinition Width="24"/></Grid.ColumnDefinitions><TextBlock Text="관경" VerticalAlignment="Center"/><TextBox Grid.Column="1" Text="{Binding DiameterMm, UpdateSourceTrigger=LostFocus}"/><TextBlock Grid.Column="2" Text="mm" Margin="4,0"/></Grid>
                <ComboBox ItemsSource="{Binding FittingTypes}" SelectedItem="{Binding SelectedFittingType}" Style="{StaticResource PrimaryComboBoxStyle}"/>
                <Button Content="+ 부속품 추가" Command="{Binding AddFittingCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,8,0,12"/>
                <ListBox ItemsSource="{Binding InlineFittings}" Height="300"><ListBox.ItemTemplate><DataTemplate><DockPanel Margin="3"><Button Content="삭제" DockPanel.Dock="Right" Command="{Binding DataContext.RemoveFittingCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/><TextBlock Text="{Binding Order}" Width="24"/><TextBlock Text="{Binding TypeKey}"/></DockPanel></DataTemplate></ListBox.ItemTemplate></ListBox>
            </StackPanel></Border>
        </Grid>
        <Grid Grid.Row="2" Margin="20,0,20,16"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Status}" VerticalAlignment="Center" Foreground="#34495E"/><StackPanel Grid.Column="1" Orientation="Horizontal"><Button Content="그리기 취소" Command="{Binding CancelDrawingCommand}" Margin="0,0,8,0"/><Button Content="초기화" Command="{Binding ClearCommand}" Margin="0,0,8,0"/><Button Content="모델 생성" Command="{Binding CreateModelCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/><Button Content="닫기" Command="{Binding CloseCommand}"/></StackPanel></Grid>
    </Grid>
</Window>
